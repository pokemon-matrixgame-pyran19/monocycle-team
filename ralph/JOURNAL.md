# 研究記録（イテレーションログ）

Ralph ループ（エージェント）が管理する。新しいエントリを**一番上**（この説明の直後）に追記する。フォーマット：

```markdown
## YYYY-MM-DD
- やったこと:
- 結果・分かったこと:
- 次にやるべきこと:
- 未解決の疑問:
```

---

## 2026-07-02（イテレーション2：02 の2択ゲームの値）
- やったこと: `theory/theory/02-power_difference/two_choice_game.md` を新規執筆（PLAN 必須2）。パワーを $P$（チーム間平均差）と $\Delta p_i,\Delta p_j$（チーム内差）に分解し、(i) $P$ の分離（値へ直行、構造・混合比に効かない）、(ii) 混合の分母 $a+d-b-c=(v_1-v_2)\times(v_3-v_4)=W$ のパワー相殺（前回の当たりを確認）、(iii) 2択条件＝各 $\Delta p$ の「窓」（中心 $m_i=\bar v_j\times(v_1-v_2)$ 等、幅は共通 $|W|$）、(iv) 2択値の閉形式 $g=P+[(v_1\times v_2)(v_3\times v_4)-m_im_j+(\Delta p_i-m_i)(\Delta p_j-m_j)]/W$、(v) 混合比のたすき掛け依存と窓の端での $0/1$ 飽和、(vi) 実質一択の場合分け（どちらの窓をどちら側に破ったかで固定単体が決まる）を導出・執筆。等パワー極限（00/team_vector の $1/(V_i\times V_j)$、01/game_value の条件）への回帰を本文に明記し、PLAN 必須3も同時完了（RULES が極限確認を導出と不可分に要求するため別イテレーションに分けなかった）。02 README の中身・記号・要点を更新。
- 結果・分かったこと: 記号は 01 が $\gamma,\delta$ を偏角に使用済みのため $\Delta p$ 系を採用。行列規約は前回の未解決疑問どおり要整理だったが、「02 は構築行列規約（team_vector / 01）に統一し、2択部分は $b\leftrightarrow c$ 不変性、一択部分は 01 の表で吸収」で解消。検算は使い捨てスクリプトの乱数20万試行（分母・窓・中心と幅・閉形式・混合比・一択の向き・$P$ 分離の全分岐一致、コミットせず）＋本文の手計算例＋等パワー極限を tools/rotating-team cli（θ=90° 相当配置、g=1.800）で突き合わせ。副産物: 窓の内側では $|\partial g/\partial\Delta p|<1/2$（発展「交換レート」の出発点になる）。
- 次にやるべきこと: PLAN 必須4「パワー差の補正ベクトル」（correction_vector.md）。有望な筋: チームごとの共通シフト $v_{3,4}\mapsto v_{3,4}+s_j$ は利得に行依存項 $v_k\times s_j$ だけを足すので、$(v_1-v_2)\times s_j=\Delta p_i$ と選べば自分のチーム内パワー差を**相手側**のシフトに吸収できる（たすき掛け吸収）。$s_i$ 側も同様に $s_i\times(v_3-v_4)=-\Delta p_j$。残差は全成分一様の定数（$P$ と同様に値へ直行）で、各シフトに1パラメータの自由度が残る。$W$ が共通シフトで不変なこととも整合。執筆時に定数の帳尻と吸収可能条件（$v_1\neq v_2$ 等）を詰めること。
- 未解決の疑問: 窓の端（弱優越・等号）や $W=0$ 退化の扱いは 00・01 と同じ精度（測度ゼロとして流す）に留めた。厳密化が必要になったら戻る。
- やったこと: `theory/theory/02-power_difference/` を新設し、README.md の骨組み（背景・中心的な問い・予定ファイル構成・記号の一覧・要点の枠・先行研究）を 00・01 の README 形式で作成。PLAN 必須の先頭タスクを完了。
- 結果・分かったこと: 予定ファイル構成はコアの問い1〜3に対応する3分割（two_choice_game.md / correction_vector.md / single_choice_threshold.md）とし、未執筆ファイルはリンクを張らず「未着手」表記にした（リンク切れ防止）。先行研究の外部リンク（mathlog / note）はループ環境から HTTP 403 で読めないため、既知結果との区別用に PLAN の「要判断」へ質問 Q1 を追記（回答なしでも導出は独立に進められる）。
- 次にやるべきこと: PLAN 必須の2番目「パワー差込みの 2×2 ゲームの値」の導出と執筆（02/two_choice_game.md）。二体構築の利得（$a=p_1-p_3+v_1\times v_3$ 等）では混合戦略値の分母 $a+d-b-c$ でパワー項が相殺しそう、という当たりがある（未検証。導出時に確認する）。
- 未解決の疑問: 一般の 2×2（4単体の $p$ が全て異なる）と、00/two_choice_game.md の行列表記と team_vector.md の構築行列の $b\leftrightarrow c$ 入れ替えの扱いをどう整理するか（導出時に明示する）。

## 2026-07-02（セットアップ）
- やったこと: Ralph ループの仕組み（ralph/ ディレクトリ一式）を導入。GOAL.md に 02（パワー差理論）の目標と完了条件を定義し、PLAN.md に初期タスクを列挙した。
- 結果・分かったこと: 研究本体はまだ手つかず。00/model.md の「パワー差の補正ベクトルは別途扱う」が 02 の出発点になる。
- 次にやるべきこと: PLAN.md 必須の先頭タスク（02-power_difference/ と README の骨組み作成）。
- 未解決の疑問: なし。
