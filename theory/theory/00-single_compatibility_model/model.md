# 単相性モデル

ポケモン $i,j$ の相性を次の行列で表す（正なら $i$ 有利、負なら $j$ 有利）。

$$
A_{ij}=p_i-p_j+x_iy_j-y_ix_j=p_i-p_j+v_i\times v_j
$$

- $p$：パワー
- $v=(x,y)$：相性ベクトル
- $v_i\times v_j=x_iy_j-y_ix_j$：2次元の外積（行列式）

## 等パワーへの絞り込み

以降は**等パワー**（構築のパワーが揃った状況）に絞る。このとき $p$ は相殺され、相性は外積項 $v_i\times v_j$ のみで決まる。パワー差の補正ベクトルは別途扱う。
